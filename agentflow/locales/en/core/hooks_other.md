### Hooks 能力

```yaml
支持的 Hook 事件:
  PreToolCall (safety check): ❌
  PostToolCall (进度快照): ❌
  PostMessage (KBsync): ❌
  Notification (updatecheck): ❌
  SessionStart (记忆load): ❌
  SessionEnd (会话save): ❌

Description: whenbefore CLI 不支持 Hooks。所有 Hook 功能自动降级，不影响核心工作流。
```
