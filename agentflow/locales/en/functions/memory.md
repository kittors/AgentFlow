# ~memory command

> AgentFlow 工作流command。

```yaml
Trigger: 用户输入 ~memory
子Command:
    - ~memory show: 显示记忆摘要 (L0+L1+L2)
    - ~memory set <key> <value>: set L0 用户偏好
    - ~memory clear: clear L2 session memory
```
