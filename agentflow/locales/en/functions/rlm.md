# ~rlm command

> AgentFlow 工作流command。

```yaml
Trigger: 用户输入 ~rlm
子Command:
    - ~rlm agents: list available rolesandsub-agent
    - ~rlm spawn <role>: 手动调用指定Role
    - ~rlm resume: restoreduring断的sub-agenttasks
    - ~rlm team: 启动 Agent Teams 协作模式
```
