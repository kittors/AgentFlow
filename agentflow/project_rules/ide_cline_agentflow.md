<!-- AGENTFLOW_ROUTER: v1.0.0 -->
- Use AgentFlow routing (R0–R4) before writing code.
- For R2/R3/R4: requirement scoring + EHRB safety check + staged execution (DESIGN → DEVELOP → KB sync).
- Keep changes minimal, run tests, and update docs/KB when applicable.
