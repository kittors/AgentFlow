# AgentFlow Environment Configuration
# Copy this file to .env and fill in your values

# LLM Provider (required)
# Supports: openai, anthropic, or any OpenAI-compatible endpoint
LLM_PROVIDER=openai
LLM_API_KEY=your-api-key-here
LLM_MODEL=gpt-4o
# LLM_BASE_URL=https://api.openai.com/v1  # Optional: custom endpoint

# Optional: separate model for lightweight tasks (summaries, extraction)
# LIGHT_MODEL=gpt-4o-mini
